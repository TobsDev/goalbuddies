import React from 'react';
import './checkinForm.scss';

const CheckinForm = () => {
    return (
        <div className="checkin-form">
            <h1>Check In</h1>
            {/* Add form elements and logic for daily check-ins */}
        </div>
    );
};

export default CheckinForm;
