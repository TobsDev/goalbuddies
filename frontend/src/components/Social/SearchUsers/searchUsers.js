import React from 'react';
import './searchUsers.scss';

const SearchUsers = () => {
    return (
        <div className="search-users">
            <h1>Search for Users</h1>
            {/* Add form elements and logic for searching users */}
        </div>
    );
};

export default SearchUsers;
