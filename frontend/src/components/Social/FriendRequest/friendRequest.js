import React from 'react';
import './friendRequest.scss';

const FriendRequest = () => {
    return (
        <div className="friend-request">
            <h1>Friend Requests</h1>
            {/* Add logic and components for displaying and managing friend requests */}
        </div>
    );
};

export default FriendRequest;
